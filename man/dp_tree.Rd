% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_tree.R
\name{dp_tree}
\alias{dp_tree}
\title{Load a D-PLACE language phylogeny}
\usage{
dp_tree(tree_id, raw = FALSE)
}
\arguments{
\item{tree_id}{A single tree ID (see [dp_trees()]).}

\item{raw}{Logical; if `TRUE`, return the raw NEXUS text instead of a
parsed tree. Useful if you don't have the ape package installed, or
want to parse the tree yourself.}
}
\value{
If `raw = FALSE` (the default), an object of class `"phylo"`
  (from the ape package), with tip labels given as Glottocodes. If
  `raw = TRUE`, a single character string of NEXUS-format tree data.
}
\description{
Load a D-PLACE language phylogeny
}
\examples{
\donttest{
tr <- dp_tree("abkh1242")
if (requireNamespace("ape", quietly = TRUE)) {
  ape::plot.phylo(tr)
}
}

}
